# 키워드 추출 모델 다운로드 가이드

## 📋 개요

이 스크립트는 HuggingFace에서 Qwen3-1.7B 모델을 다운로드하여 로컬에 저장합니다.

## 🚀 사용 방법

### 1. 스크립트 실행

```bash
cd backend/STT/call_stt
python download_keyword_model.py
```

### 2. 다운로드 진행

- **크기**: 약 3GB
- **소요 시간**: 5-10분 (인터넷 속도에 따라)
- **저장 위치**: `backend/STT/models/qwen3-1.7b/`

### 3. 다운로드 완료 후

다운로드가 완료되면 자동으로 저장되며, 이후 `server5.py` 실행 시 로컬 모델을 사용합니다.

```bash
python server5.py
```

## 📦 다운로드되는 파일

| 파일명 | 크기 | 설명 |
|--------|------|------|
| `model.safetensors` | ~3GB | 모델 가중치 (safetensors 형식) |
| `config.json` | <1MB | 모델 설정 |
| `tokenizer.json` | ~7MB | 토크나이저 |
| `vocab.json` | ~3MB | 어휘 사전 |
| `merges.txt` | ~2MB | BPE 병합 규칙 |

## ⚠️ 주의사항

1. **디스크 공간**: 최소 5GB의 여유 공간 필요
2. **인터넷 연결**: 안정적인 인터넷 연결 필요
3. **재다운로드**: 이미 모델이 있는 경우 확인 메시지 표시

## 🔧 문제 해결

### 에러: `ImportError: No module named 'transformers'`

```bash
pip install transformers torch
```

### 다운로드 중단된 경우

스크립트를 다시 실행하면 됩니다. HuggingFace는 부분 다운로드를 지원합니다.

### 모델 삭제 후 재다운로드

```bash
rm -rf backend/STT/models/qwen3-1.7b
python download_keyword_model.py
```

## 📊 다운로드 vs 온라인 로드

### 로컬 다운로드 (권장)
- ✅ 서버 시작 속도 빠름
- ✅ 인터넷 연결 불필요
- ✅ 명시적인 버전 관리
- ❌ 초기 다운로드 시간 필요

### 온라인 로드
- ✅ 초기 설정 간단
- ❌ 서버 시작 시마다 다운로드 확인
- ❌ 인터넷 연결 필수
- ❌ 첫 실행 시 느림

## 🔗 관련 정보

- **모델 페이지**: https://huggingface.co/Qwen/Qwen3-1.7B
- **라이선스**: Apache 2.0
- **용도**: 한국어 키워드 추출

